import { PreRenderPipe } from './pre-render.pipe';

describe('PreRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new PreRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
